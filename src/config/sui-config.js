/**
 * Sui Configuration Module
 * Provides dynamic Sui RPC URL from user settings
 */

import { getSuiRpcUrl } from '../utils/settings.js';

/**
 * Get the current Sui RPC URL from settings
 * @returns {string} Sui RPC URL
 */
export function getSuiRpcUrl() {
  return getSuiRpcUrl();
}
